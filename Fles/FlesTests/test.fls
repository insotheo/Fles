fn sumAndSquare(x : int, y : int){
    let sum : int = x + y;
    return (sum * sum);
}

fn finishProgram(status : int) : int{
    //... some code for finishing program ...
    return status;
}

fn main(){
    finishProgram(add(+1, -2));
}